# Verifier Strategy

## Example 1

Given:

```
package com.qux.foo {
    // anonymous block
    {
        var o:B.<Number> = new B.<Number>;
        o.x;
    }

    public class B.<T> extends A.<T> {
        public function B() {
            super();
            trace(this.x.toString(16));
        }
    } // B

    public class A.<T>
        where T is L
    {
        public var x = 10;
    } // A

    public interface L {
    } // L
}
```

This program is resolved in the following steps:

- 1. ?
- 2. ?
- 3. ?
- 4. ?
- 5. ?
- 6. ?
- 7. ?